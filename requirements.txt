# Build step (downloading from OpenStreetMap) needs osmnx.
osmnx>=1.9
# Routing + mutations (what the simulation engine consumes) need only networkx.
networkx>=3.0
# Demand model + simulation layer.
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
# Optional: the committed XGBoost demand model and GPU-accelerated training
# backend (e.g. on the ASUS GX10). The CPU demo has a deterministic fallback.
# xgboost>=2.0
