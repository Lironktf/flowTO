"""FIFA WC 2026 match-day demo (P12).

The 90-second narrative: weekday baseline → BMO Field post-match egress surge →
road-side mitigation (contraflow + signal retiming + pedestrian corridor + 509
transit hold) → headline metric improvement. Deterministic and pre-baked: the
three scenarios (``baseline``, ``wc_surge``, ``wc_fix``) run identically every
time so rehearsal == performance.
"""

from __future__ import annotations

__all__ = ["wc_surge"]
